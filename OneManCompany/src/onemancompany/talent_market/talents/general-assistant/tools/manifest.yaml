builtin_tools:
- read_file
- write_file
- list_dir
- bash
custom_tools: []
mcp_servers:
  fastskills:
    command: uvx
    args: ["fastskills", "--skills-dir", "{skills_dir}", "--workdir", "{workdir}"]
    env:
      SKILLSMP_API_KEY: "{SKILLSMP_API_KEY}"

# Frontend Code Engineering Development Standards
To improve project maintainability, all frontend development tasks must strictly follow engineering standards:
Proactively separate page structure (HTML), visual styling (CSS), and business logic (JS) into independent source files. Consolidating all code into a single HTML file is strictly prohibited.
